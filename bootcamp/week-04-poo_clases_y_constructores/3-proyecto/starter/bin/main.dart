// ============================================
// Catálogo de Objetos con Clases — Proyecto Semana 4
// ============================================
//
// NOTA PARA EL APRENDIZ:
// Completa primero lib/models/item.dart y lib/repository/item_repository.dart
// antes de descomentar este archivo. Ajusta los imports al nombre de clase
// que hayas usado.
//
// import 'package:catalogo_de_objetos/models/item.dart';
// import 'package:catalogo_de_objetos/repository/item_repository.dart';

void main() {
  print('=== Catálogo de Objetos ===');

  // TODO: crea un ItemRepository y agrega al menos 4 items, usando AL MENOS
  // dos constructores distintos (normal, nombrado, factory).
  // final repository = ItemRepository();
  //
  // repository.add(Item('B1', 'Clean Code', 3));
  // repository.add(Item.outOfStock('B2', '1984'));
  // repository.add(Item.fromRecord({'id': 'B3', 'name': 'Dune', 'stock': '5'}));
  // repository.add(Item('B4', 'El Principito', 2));

  // TODO: imprime cada item disponible (usa availableItems()).
  // for (final item in repository.availableItems()) {
  //   print(item);
  // }

  // TODO: imprime el stock total del repositorio y el total de instancias
  // creadas (Item.totalCreated).
  // print('Stock total: ${repository.totalStock()}');
  // print('Items creados: ${Item.totalCreated}');
}
