// ============================================
// Ficha de Dominio en Consola — Proyecto Semana 1
// ============================================
//
// NOTA PARA EL APRENDIZ:
// Adapta las variables de abajo a UN elemento de tu dominio asignado.
// Ejemplos:
// - Biblioteca: title, author, available, dueDate
// - Farmacia: name, laboratory, inStock, expirationDate
// - Gimnasio: memberName, plan, active, renewalDate
//
// Esta semana solo viste variables, tipos y null safety — todavía no hay
// funciones propias, clases ni colecciones. El proyecto se queda a
// propósito dentro de ese alcance: todo el programa vive en main().

void main() {
  // TODO: declara al menos 4 variables que describan UN elemento de tu
  // dominio, usando el modificador correcto en cada caso:
  //   - `final` para datos que no cambian tras asignarse (ej. un título o id)
  //   - `const` para un valor literal conocido en tiempo de compilación
  //     (ej. una tarifa fija, un límite del negocio)
  //   - `var` solo si de verdad necesitas reasignar el valor más adelante
  // Incluye tipos explícitos donde aporte claridad: String, int, double, bool.

  // TODO: declara UNA variable nullable (tipo con `?`) que represente un
  // dato que puede legítimamente no existir para este elemento (ej. una
  // fecha de devolución si el ítem no está prestado, una calificación si
  // nadie la ha dado todavía).

  // TODO: usa el operador `??` para dar un valor por defecto legible al
  // imprimir esa variable nullable (ej. 'Sin calificación aún').

  // TODO: imprime una "ficha" formateada de tu elemento usando
  // interpolación de strings ($variable o ${expresión}), con al menos
  // 4 líneas de salida además del encabezado.

  print('=== Ficha de Dominio ===');
}
