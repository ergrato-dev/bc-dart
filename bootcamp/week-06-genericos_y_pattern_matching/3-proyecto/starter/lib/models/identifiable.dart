// ============================================
// MODEL: Identifiable
// Bound genérico para Repository<T extends Identifiable>
// ============================================

// TODO: declara `Identifiable` como clase ABSTRACTA con un getter `id` (String).
// Cualquier tipo que uses con Repository<T> deberá implementar este contrato.
//
// abstract class Identifiable {
//   String get id;
// }
