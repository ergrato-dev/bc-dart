name: catalogo_de_elementos
description: Catálogo de elementos en lista — proyecto semana 3 (Dart puro, sin Flutter)
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dev_dependencies:
  lints: ^5.0.0
